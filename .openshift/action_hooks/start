TRACDIR=trac
echo "Starting TRAC:"
echo "$OPENSHIFT_REPO_DIR$TRACDIR/tracd -b $OPENSHIFT_INTERNAL_IP -p 8080 -s $OPENSHIFT_DATA_DIR$OPENSHIFT_APP_NAME"
$OPENSHIFT_REPO_DIR$TRACDIR/tracd -b $OPENSHIFT_INTERNAL_IP -p $OPENSHIFT_INTERNAL_PORT -s $OPENSHIFT_DATA_DIR$OPENSHIFT_APP_NAME -d --pidfile=$OPENSHIFT_REPO_DIR$TRACDIR/trac.pid


